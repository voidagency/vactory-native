{
  "name": "js",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:mobile": "yarn workspace mobile dev",
    "lint": "yarn workspace @vactory/ui lint",
    "format": "prettier --write \"**/*.{js,jsx,md}\"",
    "clean": "rm -rf node_modules"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier-config-custom": "*",
    "prettier": "^2.8.4"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.19"
}